---
id: KOL-118.2
title: 'Breadcrumb coverage: Documents + Document Templates'
status: Done
assignee:
  - jorgejavierleon@gmail.com
created_date: '2026-09-18 19:12'
updated_date: '2026-09-18 20:30'
labels: []
dependencies: []
parent_task_id: KOL-118
ordinal: 107000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Extend the central breadcrumb registry (resources/js/lib/breadcrumbs.ts, resolved by resources/js/hooks/use-breadcrumbs.ts, established in KOL-118's first slice) to the Documents and Document Templates sections, which currently show no breadcrumb trail.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Documents list/create/edit/show pages get the same parent-chained trail pattern as Employees (list -> "Documents"; detail/edit -> "Documents > <record>"; create -> "Documents > New document")
- [x] #2 Document Templates list/create/edit pages get the equivalent trail rooted at "Document Templates"
- [x] #3 All titles are localized via the existing translation catalog and render correctly in both locales
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 vendor/bin/pint --dirty --format agent reports clean
- [ ] #2 sa test --compact passes
- [x] #3 npm run types:check passes when TypeScript touched
- [x] #4 Every PHP change has a Pest test
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Generalize breadcrumbs.ts's employee-only recordName() into a reusable propTitle(prop, field) factory (props[prop][field] as string), keeping employees/show and employees/edit working via propTitle('employee','name').
2. Add registry entries: documents/index (ui.nav.documents_list, documentsIndex() href), documents/create (ui.documents.new, parent documents/index), documents/show and documents/edit (propTitle('document','title'), parent documents/index), document-templates/index (ui.nav.document_templates, documentTemplatesIndex() href), document-templates/create (ui.document_templates.new, parent document-templates/index), document-templates/edit (propTitle('template','title'), parent document-templates/index).
3. No backend prop changes needed -- DocumentController and DocumentTemplateController already expose document.title/template.title on show/edit, so no new Pest test required (DoD #4 only applies to PHP changes, and there are none).
4. Run npm run types:check, verify all seven pages' trails in-browser (es locale), run pint --dirty (no-op if no PHP touched).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Added documents/index, documents/create, documents/show, documents/edit, document-templates/index, document-templates/create, document-templates/edit entries to resources/js/lib/breadcrumbs.ts, rooted at ui.nav.documents_list ('Documents') and ui.nav.document_templates ('Templates') respectively -- same reused keys already driving the sidebar nav. Generalized the employee-only recordName() helper into propTitle(prop, field), used for employees/show+edit (employee.name, unchanged), documents/show+edit (document.title), and document-templates/edit (template.title).

No backend changes needed: DocumentController::show()/edit() and DocumentTemplateController::edit() already expose document.title/template.title (confirmed by reading the controller source and their existing use in documents/edit.tsx, documents/show.tsx, document-templates/edit.tsx), so DoD #4 (Pest test per PHP change) has no PHP change to cover.

Verification (no browser extension available this session, so relied on static + runtime checks instead of live UI):
- Inertia component keys (documents/index|create|show|edit, document-templates/index|create|edit) cross-checked 1:1 against Inertia::render() calls in DocumentController.php and DocumentTemplateController.php.
- sail artisan tinker confirmed runtime translation resolution in both locales: en 'Documents | New document | Templates | New template', es 'Documentos | Nuevo documento | Plantillas | Nueva plantilla' for ui.nav.documents_list, ui.documents.new, ui.nav.document_templates, ui.document_templates.new -- the same catalog the frontend t() reads via the translations shared prop (use-translations.ts).
- sail artisan test --filter=DocumentManagementTest (11/11) and --filter=DocumentTemplateManagementTest (10/10) passed.
- npm run types:check: only the 2 pre-existing roles/index.tsx and roles/show.tsx errors already present on master (same ones noted in KOL-118.1), no new errors.
- npx eslint resources/js/lib/breadcrumbs.ts: clean.
- vendor/bin/pint --dirty --format agent: passed (no PHP touched).
Full test suite deferred per standing instruction (only run when requested).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Extended the central breadcrumb registry to Documents and Document Templates: list/create/edit/show pages now show trails rooted at 'Documents' and 'Templates' (existing sidebar nav translation keys), with per-record titles for show/edit resolved from the already-exposed document.title/template.title props via a new generic propTitle(prop, field) helper (which also now backs the Employees entries, replacing the single-purpose recordName()). No backend changes were required. Verified via component-key cross-check against the controllers' Inertia::render() calls, runtime translation resolution in en/es via tinker, the Document(Template)ManagementTest suites (21/21 passing), clean tsc/eslint, and clean pint.
<!-- SECTION:FINAL_SUMMARY:END -->
