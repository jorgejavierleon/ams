---
id: KOL-123.1
title: 'Settings: Notificaciones page + shared nav scaffolding'
status: In Review
assignee:
  - '@jorgejavierleon'
created_date: '2026-09-22 08:58'
updated_date: '2026-09-22 09:10'
labels: []
dependencies: []
parent_task_id: KOL-123
ordinal: 121000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
First slice of KOL-123. Extracts the notifications section of the current /organization-settings page into resources/js/pages/settings/notifications.tsx, and lays down the shared scaffolding the other two sections (Documentos, Horas extras) will reuse:
- Add a left-nav item to resources/js/layouts/settings/layout.tsx, visible only to admins (same permission check app-sidebar.tsx already uses to split employee vs admin nav groups).
- Repoint the app-sidebar's existing 'Configuración general' shortcut at this new page instead of /organization-settings.
- Add the breadcrumb registry entry (resources/js/lib/breadcrumbs.ts) under the existing virtual 'settings' parent, same as settings/profile etc.
- New backend route + controller for GET/PATCH (validating only the notification boolean fields: employee_missing_in_notification, employee_missing_out_notification, employer_missing_in_notification, employer_missing_out_notification, leave_approval_notification), reusing App\Services\OrganizationSettings.
- The old /organization-settings page keeps working unchanged until the last slice removes it.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 GET the new route as an admin renders a page titled Notificaciones with the five notification toggles, matching current values from OrganizationSettings
- [x] #2 Saving the form PATCHes only the notification fields and persists
- [x] #3 A non-admin gets a 403 on both GET and PATCH
- [ ] #4 The Settings left nav shows Notificaciones for admins and not for non-admins, styled like Perfil/Seguridad/Apariencia
- [ ] #5 The app-sidebar shortcut and the breadcrumb trail point at the new page
- [x] #6 Pest coverage mirrors the notification-related cases from tests/Feature/OrganizationSettingsTest.php
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 vendor/bin/pint --dirty --format agent reports clean
- [ ] #2 sa test --compact passes
- [x] #3 npm run types:check passes when TypeScript touched
- [x] #4 Every PHP change has a Pest test
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Backend: add Settings/NotificationController (edit/update) reusing OrganizationSettings::current(), validating the 5 notification boolean fields. Route names settings-notifications.edit/.update in routes/settings.php under auth+role:admin, path /settings/notifications.
2. Frontend: resources/js/pages/settings/notifications.tsx (mirrors security.tsx style: small Heading + form with the 5 SettingToggle rows, reusing the same toggle UI as organization-settings.tsx).
3. SettingsLayout: add a Notificaciones nav item, shown only when !isEmployee (mirror app-sidebar's auth.permissions.includes('ViewOwn:Leave') check).
4. app-sidebar.tsx: repoint the 'Configuración general' shortcut at settings-notifications edit route.
5. breadcrumbs.ts: add 'settings/notifications' entry under the existing virtual 'settings' parent; add ui.settings.nav.notifications translation key.
6. lang/en+es ui.php: add ui.settings.notifications.{title,description,fields.*,flash.updated} (copied text from ui.organization_settings.*).
7. Tests: new tests/Feature/Settings/NotificationSettingsTest.php mirroring the notification-related cases from OrganizationSettingsTest.php (access control, view, update+persist, cache invalidation, org scoping, validation, translation coverage).
8. Run wayfinder generation (npm run build or dev) after adding routes so @/routes/settings-notifications exists.
9. pint --dirty, sa test --compact --filter=NotificationSettingsTest, npm run types:check.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verified: NotificationSettingsTest.php (8 tests, 41 assertions) covers access control (403 for non-admin GET/PATCH, redirect when unauthenticated), edit rendering with defaults, update+persistence, cache invalidation on save, org scoping, boolean validation, and Spanish label coverage — all passing. pint --dirty clean. npm run types:check clean on all touched files (2 pre-existing unrelated errors in roles/index.tsx and roles/show.tsx, untouched by this change). Wayfinder regenerated with --with-form for the new settings-notifications route.
AC #4 (nav shows Notificaciones for admins, hidden for employees) and AC #5 (app-sidebar shortcut + breadcrumb point at the new page) are implemented by code (SettingsLayout gated on the same !isEmployee check as app-sidebar; app-sidebar shortcut repointed; breadcrumbs.ts entry added) but not visually verified — the Claude-in-Chrome browser extension was not connected this session. Logged to docs/QA_CHECKLIST.md for manual verification; leaving those two ACs unchecked until confirmed.
Full 'sa test --compact' suite not run per standing instruction to only run filtered/relevant tests during ticket work and defer the full suite until the user reviews and asks for it.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added the Settings: Notificaciones page (/settings/notifications) as the first vertical slice of KOL-123, plus the shared nav/breadcrumb scaffolding the other two subtasks build on: Settings\NotificationController (edit/update, admin-gated), routes/settings.php settings-notifications.{edit,update}, resources/js/pages/settings/notifications.tsx, a reusable SettingToggle component, an admin-only nav item in SettingsLayout, the app-sidebar shortcut repointed, and a breadcrumbs.ts entry. Verified via NotificationSettingsTest.php (8 passing tests), pint, and tsc. Nav-visibility and shortcut/breadcrumb navigation (AC #4, #5) still need a manual browser check — queued in docs/QA_CHECKLIST.md since the browser tool was unavailable this session.
<!-- SECTION:FINAL_SUMMARY:END -->
