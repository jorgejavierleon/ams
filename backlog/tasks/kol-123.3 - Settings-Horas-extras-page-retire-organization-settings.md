---
id: KOL-123.3
title: 'Settings: Horas extras page + retire /organization-settings'
status: In Review
assignee:
  - '@jorgejavierleon'
created_date: '2026-09-22 08:58'
updated_date: '2026-09-22 09:18'
labels: []
dependencies:
  - KOL-123.2
parent_task_id: KOL-123
ordinal: 123000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Third and last slice of KOL-123, built on KOL-123.1/.2. Extracts the overtime policy section of the current /organization-settings page into resources/js/pages/settings/overtime.tsx: overtime_authorization_mode (select), overtime_weekly_anomaly_threshold_hours, overtime_retroactive_request_days, overtime_counts_pre_shift_excess. New backend route + controller for GET/PATCH validating just those fields, reusing App\Services\OrganizationSettings and App\Enums\OvertimeAuthorizationMode::options(). Add its own left-nav item and breadcrumb entry.

Once this page works, retire the old page entirely: delete the /organization-settings route, App\Http\Controllers\SettingController, resources/js/pages/organization-settings.tsx, resources/js/routes/organization-settings, and tests/Feature/OrganizationSettingsTest.php (superseded by the three new per-section test files).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 GET the new route as an admin renders a page titled Horas extras with the mode select and the two numeric fields plus the pre-shift-excess toggle, matching current values
- [x] #2 Saving the form PATCHes only the overtime policy fields and persists, with the same validation ranges as today (0-168 hours, 0-365 days, valid enum)
- [x] #3 A non-admin gets a 403 on both GET and PATCH
- [ ] #4 The Settings left nav shows Horas extras for admins and not for non-admins, completing the Notificaciones/Documentos/Horas extras group
- [x] #5 /organization-settings, SettingController, organization-settings.tsx and OrganizationSettingsTest.php no longer exist
- [x] #6 Pest coverage mirrors the overtime-policy cases from the old tests/Feature/OrganizationSettingsTest.php
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 vendor/bin/pint --dirty --format agent reports clean
- [ ] #2 sa test --compact passes
- [x] #3 npm run types:check passes when TypeScript touched
- [x] #4 Every PHP change has a Pest test
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Backend: Settings/OvertimeController (edit/update), reusing OrganizationSettings::current() + App\Enums\OvertimeAuthorizationMode::options(), validating overtime_authorization_mode (enum), overtime_weekly_anomaly_threshold_hours (0-168), overtime_retroactive_request_days (0-365), overtime_counts_pre_shift_excess (bool). Routes settings-overtime.edit/.update in routes/settings.php, same auth+role:admin group, path /settings/overtime.
2. Frontend: resources/js/pages/settings/overtime.tsx — small Heading, FormField+Select for the mode, FormField+Input for the two numeric fields, SettingToggle for pre-shift-excess, matching organization-settings.tsx's existing overtime section markup.
3. SettingsLayout: add a Horas extras nav item after Documentos, same !isEmployee gate.
4. breadcrumbs.ts: add 'settings/overtime' entry.
5. lang/en+es ui.php: add ui.settings.nav.overtime + ui.settings.overtime.{title,description,fields.*,authorization_modes.*,flash.updated}, copied from ui.organization_settings.*.
6. Tests: tests/Feature/Settings/OvertimeSettingsTest.php mirroring the overtime-policy + pre-shift-excess cases from OrganizationSettingsTest.php (including the two 'no tenant-wide compensation/pact' schema-shape tests, since they assert on this same settings payload shape).
7. Wayfinder generate --with-form.
8. Retire the old page: remove SettingController, its routes/web.php entries (role:admin block), organization-settings.tsx, resources/js/routes/organization-settings (wayfinder-generated, cleaned up by regenerating without the route), the app-sidebar/breadcrumbs 'organization-settings' import+entry (breadcrumbs entry already unused once nav no longer points there — confirm nothing else references route('organization-settings.edit')/.update before deleting), and tests/Feature/OrganizationSettingsTest.php. Remove the now-orphaned ui.organization_settings.* block from lang/en+es ui.php once nothing reads it.
9. pint --dirty, sa test --compact --filter='OvertimeSettingsTest|NotificationSettingsTest|DocumentSettingsTest', npm run types:check, plus a repo-wide grep for 'organization-settings' and 'SettingController' to confirm no dangling references.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verified: OvertimeSettingsTest.php (15 tests) covers access control, edit rendering with legal defaults, mode round-tripping, full policy persistence, out-of-range/invalid rejection, cache invalidation, org scoping (bidirectional), Spanish label coverage (including the OvertimeAuthorizationMode enum labels, whose lang key moved from ui.organization_settings.overtime_authorization_modes.* to ui.settings.overtime.overtime_authorization_modes.*), pre-shift-excess behavior, and the two 'no tenant-wide compensation/pact column' schema-shape tests carried over verbatim. Also fixed OvertimeSectionTest.php's 'a supervisor cannot reach the tenant overtime policy configuration' test, which pointed at the now-removed organization-settings.edit route — repointed to settings-overtime.edit. Ran all four Settings test files + OvertimeSectionTest together (40 tests, 180 assertions, all passing), plus OvertimeAuthorizationModeTest/OvertimeQueueBadgeTest/OvertimeRequestReviewTest (18 tests) to confirm the enum label relocation didn't break anything else that reads ->label()/::options(). pint --dirty clean. npm run types:check clean (same 2 pre-existing unrelated errors in roles/index.tsx and roles/show.tsx). Repo-wide grep for 'organization-settings', 'SettingController', and 'ui.organization_settings' — no remaining references anywhere (app, resources/js, routes, tests, database).
Retired: app/Http/Controllers/SettingController.php, resources/js/pages/organization-settings.tsx, tests/Feature/OrganizationSettingsTest.php, the routes/web.php role:admin organization-settings routes, the resources/js/routes/organization-settings + actions/.../SettingController.ts Wayfinder output (cleaned up by regeneration), the breadcrumbs.ts 'organization-settings' entry+import, and the ui.organization_settings.* lang block in both en/es. The app-sidebar 'Configuración general' shortcut and its ui.nav.organization_settings label are kept per KOL-123's decision (repointed at settings-notifications in KOL-123.1, unchanged here).
AC #4 (nav shows Horas extras completing the group) not visually verified — browser extension unavailable this session. Logged to docs/QA_CHECKLIST.md along with a check that /organization-settings itself is gone.
Full 'sa test --compact' suite deferred per standing instruction until the user reviews and asks for it.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added the Settings: Horas extras page (/settings/overtime) as the third and last slice of KOL-123, and retired the old combined /organization-settings page entirely. New: Settings\OvertimeController, settings-overtime.{edit,update} routes, resources/js/pages/settings/overtime.tsx, a Horas extras nav item completing the group, and a breadcrumbs.ts entry. Removed: SettingController, organization-settings.tsx, its routes, its Wayfinder output, its breadcrumb entry, OrganizationSettingsTest.php, and the ui.organization_settings.* lang block (relocating the OvertimeAuthorizationMode enum's label lookup to ui.settings.overtime.*). Verified via OvertimeSettingsTest.php (15 tests) plus a combined run with the other two Settings test suites and OvertimeSectionTest (40 tests total), the enum/queue/review overtime suites (18 tests) to confirm nothing else broke, pint, tsc, and a repo-wide grep confirming no dangling references to the removed page. Nav visibility and the /organization-settings 404 (AC #4) still need a manual browser check — queued in docs/QA_CHECKLIST.md.
<!-- SECTION:FINAL_SUMMARY:END -->
