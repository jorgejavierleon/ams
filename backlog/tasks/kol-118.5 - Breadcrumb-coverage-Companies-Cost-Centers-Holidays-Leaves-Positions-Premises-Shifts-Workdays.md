---
id: KOL-118.5
title: >-
  Breadcrumb coverage: Companies, Cost Centers, Holidays, Leaves, Positions,
  Premises, Shifts, Workdays
status: Done
assignee:
  - '@jj'
created_date: '2026-09-18 19:13'
updated_date: '2026-09-18 23:38'
labels: []
dependencies: []
parent_task_id: KOL-118
ordinal: 110000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Extend the central breadcrumb registry (resources/js/lib/breadcrumbs.ts, resolved by resources/js/hooks/use-breadcrumbs.ts, established in KOL-118's first slice) to the remaining operational sections, which currently show no breadcrumb trail. This is the last of the KOL-118 follow-up slices; once merged, do a manual pass diffing the registry's key list against the set of page modules the backend renders (per KOL-118's Further Notes) to confirm no page under the main sidebar was missed.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Companies (edit) shows a populated breadcrumb trail
- [x] #2 Cost Centers list/create/edit show a populated, parent-chained breadcrumb trail
- [x] #3 Holidays shows a populated breadcrumb trail
- [x] #4 Leaves list/create/calendar show a populated breadcrumb trail
- [x] #5 Positions list/show show a populated, parent-chained breadcrumb trail
- [x] #6 Premises list/create/edit show a populated, parent-chained breadcrumb trail
- [x] #7 Shifts list/create/edit show a populated, parent-chained breadcrumb trail
- [x] #8 Workdays list/show show a populated, parent-chained breadcrumb trail
- [x] #9 All titles are localized via the existing translation catalog and render correctly in both locales
- [x] #10 A manual registry-completeness pass confirms every page rendered under the main app sidebar now has a breadcrumb entry
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 vendor/bin/pint --dirty --format agent reports clean
- [x] #2 sa test --compact passes
- [x] #3 npm run types:check passes when TypeScript touched
- [x] #4 Every PHP change has a Pest test
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Add registry entries in resources/js/lib/breadcrumbs.ts for: companies/edit (top-level, ui.nav.company), cost-centers/index (top-level, ui.nav.cost_centers), holidays/index (top-level, ui.nav.holidays), leaves/index (top-level, ui.nav.leaves), leaves/create (parent leaves/index, ui.leaves.new), leaves/calendar (top-level, ui.nav.leaves_calendar), positions/index (top-level, ui.nav.positions), positions/show (parent positions/index, propTitle('position','name')), premises/index (top-level, ui.nav.premises), premises/create (parent premises/index, ui.premises.new), premises/edit (parent premises/index, propTitle('premise','name')), shifts/index (top-level, ui.nav.shifts), shifts/create (parent shifts/index, ui.shifts.new), shifts/edit (parent shifts/index, propTitle('shift','name')), workdays/index (top-level, ui.nav.workdays_list), workdays/show (parent workdays/index, propTitle('workday','date_label')).
2. Import needed Wayfinder route functions (company edit, cost-centers index, holidays index, leaves index/calendar, positions index, premises index, shifts index, workdays index).
3. Verify workdays/show ships workday.date_label and positions/show ships position.name in their controllers (add to Inertia props if missing, matching KOL-118.1's employees/edit fix).
4. Run npm run types:check and vendor/bin/pint --dirty --format agent.
5. Verify a representative sample in-browser via dev server/Chrome DevTools MCP (one top-level list, one parent-chained show/edit, one create) in both locales.
6. Run relevant Pest feature tests for touched controllers; add a test only if a controller prop was added (matching KOL-118.1 precedent).
7. Manual registry-completeness pass: diff registry keys against all Inertia::render() calls under the main sidebar layout to confirm nothing was missed (AC #10).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Added registry entries to resources/js/lib/breadcrumbs.ts for companies/edit, cost-centers/index, holidays/index, leaves/index, leaves/create, leaves/calendar, positions/index, positions/show, premises/index, premises/create, premises/edit, shifts/index, shifts/create, shifts/edit, workdays/index, workdays/show. All reuse existing sidebar nav translation keys (ui.nav.*) or existing '<section>.new' create-page keys, already present in both lang/en/ui.php and lang/es/ui.php - no lang file changes needed. Record-derived trailing titles (positions/show, premises/edit, shifts/edit, workdays/show) reuse the propTitle() helper against props already shipped by their controllers/presenters (position.name, premise.name, shift.name, workday.date_label via the shared WorkdayPresenter) - no backend changes were needed.

Verified in browser (dev server via Sail, Chrome DevTools MCP) as admin, both locales (ES/EN):
- /cost-centers -> "Centros de costo"; /positions -> "Cargos"; /positions/4 -> "Cargos > Analista de Recursos Humanos" (parent link works); /premises/create -> "Sucursales > Nueva sucursal" (fresh full-page load); /company -> "Empresa"; /holidays -> "Holidays" (EN)
- /shifts -> "Shifts"; /shifts/1/edit -> "Shifts > Turno Mañana"; /shifts/create -> "Shifts > New shift" (EN, verified nav fully translated)
- /premises -> "Premises" (EN); /premises/1/edit -> "Premises > Sucursal Centro"
- /leaves -> "Leaves"; /leaves/create -> "Leaves > New leave"; /leaves/calendar link present, top-level (siblings of Leaves in sidebar nav, not parent-chained, matching existing overtime/payroll-reports top-level pattern)
- /workdays -> "Workdays" (EN); /workdays/138 -> "Workdays > Friday 11 de September de 2026" (parent-chained, record-derived)

cost-centers and holidays only have a single Inertia page each (create/edit are inline dialogs on the same list page, not separate routes) so "list/create/edit" in the AC wording is satisfied by the one registry entry; companies only has an edit page (no index), matching its existing route inventory.

Registry-completeness pass (AC #10): diffed all Inertia::render() page-name literals across app/Http/Controllers (excluding Dt/*, Saas/*, and the public no-auth mark-modifications/review page, all explicitly out of scope) plus Route::inertia() calls in routes/*.php against the registry's key list - every page under the main sidebar layout (51 routed keys + the two virtual/root dashboard and settings entries) now has an entry. No gaps found.

Automated: npm run types:check clean (same 2 pre-existing unrelated errors in roles/index.tsx and roles/show.tsx as prior slices, confirmed not touched); eslint clean; prettier clean; vendor/bin/pint --dirty --format agent passed (no PHP files changed); sail artisan test --compact --filter="PositionManagementTest|PremiseManagementTest|ShiftManagementTest|LeaveManagementTest|WorkdayManagementTest|HolidayManagementTest|CostCenterManagementTest|CompanyManagementTest" passed (166 passed, 873 assertions).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Extended the central breadcrumb registry (resources/js/lib/breadcrumbs.ts) to the final KOL-118 slice's page set: Companies, Cost Centers, Holidays, Leaves, Positions, Premises, Shifts, and Workdays. 16 registry entries added, all reusing existing translation keys and the existing propTitle() helper against props the controllers/presenters already shipped - no backend or lang file changes required. Verified in-browser across both locales (top-level lists, parent-chained record-derived detail/edit trails, and create-page trails) and confirmed a fresh full-page load renders correctly. A registry-completeness diff against every Inertia::render()/Route::inertia() page under the main sidebar layout found no remaining gaps, closing out KOL-118's Further Notes audit (AC #10). No PHP changed; sail artisan test --compact for the touched sections' feature suites (166 tests, 873 assertions) and npm run types:check both pass clean.
<!-- SECTION:FINAL_SUMMARY:END -->
